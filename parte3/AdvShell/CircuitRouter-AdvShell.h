#ifndef CIRCUITROUTER_ADVSHELL_H
#define CIRCUITROUTER_ADVSHELL_H 1

enum {
	ERR_LINEARGS,
	ERR_COMMANDS,
	ERR_FORK
};
/* =============================================================================
 * shell_executeInstructions
 * =============================================================================
 */
void shell_executeInstructions();

/* =============================================================================
 * shell_manageSignals
 * =============================================================================
 */
void* shell_manageSignals(void* argPtr);

/* =============================================================================
 * shell_manageStdin
 * =============================================================================
 */
void* shell_manageStdin(void* argPtr);

/* =============================================================================
 * shell_managePipe
 * =============================================================================
 */
void* shell_managePipe(void* argPtr);

/* =============================================================================
 * shell_pushInstruction
 * =============================================================================
 */
void shell_pushInstruction(char* circuitName, char* clientPipeName);

/* =============================================================================
 * shell_waitForAll
 * =============================================================================
 */
void shell_waitForAll();

/* =============================================================================
 * shell_signalHandler
 * =============================================================================
 */
void shell_signalHandler(int sig);

#endif /* CIRCUITROUTER_ADVSHELL_H */


/* =============================================================================
 *
 * End of CircuitRouter-AdvShell.h
 *
 * =============================================================================
 */
